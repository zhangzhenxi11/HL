我将执行以下任务来满足您的新需求，并完成之前的工艺时间逻辑更新。

### 计划：

1.  **修改文件保存/加载逻辑 (`device/src/pm_recipe_widget.cpp`)**:
    *   引入 `<QFileDialog>` 头文件。
    *   **`onLoadParameters`**: 使用 `QFileDialog::getOpenFileName` 替代硬编码路径，允许用户选择 JSON 文件。
    *   **`onSetParameters`**: 使用 `QFileDialog::getSaveFileName` 替代硬编码路径，允许用户命名并保存文件。

2.  **更新工艺时间执行逻辑 (`device/src/pm_recipe_widget.cpp`)**:
    *   **修改 `runTimer`**: 将参数单位从“分钟”改为“秒”（乘以 1000.0 计算毫秒），以支持秒级精度。
    *   **修改 `onStartCycle`**:
        *   获取 `totalTime` (原 `process_time_min` 字段，现代表总秒数) 和 `lastTime` (`last_process_time_s`)。
        *   在 Process 步骤的循环中：
            *   若当前是最后一次循环 (`idx == count - 1`)：等待时间 = `lastTime`。
            *   若不是最后一次：等待时间 = `(totalTime - lastTime) / (count - 1)`。
        *   调用更新后的 `runTimer` 执行等待。

### 待办事项更新：
我将更新现有的 Todo 列表，合并新需求。