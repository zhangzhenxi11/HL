## 目标
- 不改现有 .ui 文件，通过最小代码改动修复 pm_recipe_widget.cpp 的编译错误，并完成工程编译

## 精确修复项
1) 头文件声明补全（d:\HLPrj\HL\device\include\pm_recipe_widget.h）
- 在 QPmRecipeWidget 类中新增以下私有成员函数声明：
  - void initPMCavityParamEdieTableWidget();
  - void addAnPMItem(const QString name);
  - void addEditTableWidgetItemDoubleSpinBox(int row, int column, double min_value, double max_value, double single_step, double value, int decimals_value = 3);
  - void addEditTableWidgetItemComboBox(int row, int column, int value);
- 目的：确保源文件中的新增函数被识别为类成员，消除作用域错乱导致的语法错误与“非法 continue”

2) 源文件包含与列数一致性（d:\HLPrj\HL\device\src\pm_recipe_widget.cpp）
- 补充头文件：#include <chrono>、#include <QSpinBox>
- 表格列数统一为 8：将 setColumnCount(9) 改为 setColumnCount(8)，保持与当前 8 项表头一致；删除所有第 9 列（rotationAngle）相关的默认值与读写（暂不启用角度列）

3) UI 绑定（不改 .ui，代码中注入按钮）
- 删除对不存在对象名的绑定：移除 connect(d->ui->load_prams_pbt/clear_prams_pbt/save_prams_pbt,...)
- 改为：
  - connect(d->ui->pushButton, &QPushButton::clicked, this, &QPmRecipeWidget::onSetParameters);
  - connect(d->ui->clear_sequence_pbt, &QPushButton::clicked, this, &QPmRecipeWidget::onClearParameters);
- 代码中新增一个“加载参数”按钮并插入到 horizontalLayout：
  - auto loadBtn = new QPushButton("加载参数", this);
  - d->ui->horizontalLayout->insertWidget(0, loadBtn);
  - connect(loadBtn, &QPushButton::clicked, this, &QPmRecipeWidget::onLoadParameters);
- 这样无需修改 .ui 文件即可完成 3 个功能绑定

4) 括号配对与循环语义核查
- 逐函数核对 onLoadParameters/onSetParameters/onAddAnItem 等大括号配对，确保所有 continue 在 for 内（当前逻辑即如此）

## 编译与验证
- 使用 CMake 生成 VS 解决方案（x64），构建 device 模块与整体工程
- 运行应用：
  - 验证“加载参数/清空参数/保存参数”按钮工作正常
  - 新增/删除行、JSON 读写正常；Start Cycle 运行并显示工艺次数

## 备注
- 如后续需要启用 rotationAngle 列，届时将：
  - 恢复列数为 9、增加表头与读写字段
  - 在循环逻辑中启用旋转命令（已预留）

## 执行请求
- 如同意上述“只改代码、不改 .ui”方案，我将立即应用修复并编译验证