好的，明白了。总时间是外部输入（这里模拟手动输入），最后一次时间也是用户设定。核心在于计算并显示前n-1次的时间，且精度保留到毫秒（小数点后3位）。

### 最终计划：

1.  **修改 UI (`device/src/pm_recipe_widget.ui`)**:
    *   将 **"工艺时间/min"** (Col 6) 标题修改为 **"总工艺时间/s"**。
    *   在表格末尾增加两列：
        *   **Col 8: "最后一次时间/s"** (可编辑，DoubleSpinBox, 精度3位)。
        *   **Col 9: "前n-1次时间/s"** (只读，DoubleSpinBox, Disabled, 精度3位)。

2.  **修改代码逻辑 (`device/src/pm_recipe_widget.cpp`)**:
    *   **UI初始化**: 更新 `addAnPMItem`，初始化 Col 8 和 Col 9。
    *   **信号连接**: 连接 Col 3 (次数), Col 6 (总时间), Col 8 (最后一次时间) 的 `valueChanged` 信号到一个新的槽函数 `updateProcessTimeDistribution`。
    *   **计算逻辑 (`updateProcessTimeDistribution`)**:
        *   获取 `total_time` (Col 6), `count` (Col 3), `last_time` (Col 8)。
        *   如果 `count > 1`: `avg_time = (total_time - last_time) / (count - 1)`。
        *   如果 `count <= 1`: `avg_time = 0` (或者等于 total_time，视逻辑而定，但只有1次时不存在前n-1次)。
        *   将 `avg_time` 设置到 Col 9，保留3位小数。
    *   **JSON序列化**:
        *   `PMParams` 增加 `double last_process_time_s`。
        *   在 `onLoadParameters` 和 `onSetParameters` 中处理新字段。
        *   注意：`process_time_min` 字段逻辑上改为秒，建议代码中变量名若不改则需加注释，或者重构变量名（推荐重构为 `process_time_s` 以免混淆）。为减少改动风险，暂保持变量名但逻辑按秒处理。

3.  **修改运行逻辑 (`onStartCycle`)**:
    *   读取 `total_time` 和 `last_time`。
    *   计算 `avg_time`。
    *   循环执行时，前 `count-1` 次使用 `avg_time`，最后一次使用 `last_time`。
    *   `runTimer` 函数的参数单位需要确认（原代码是 `minutes`，现在输入是 `seconds`），需要调整 `runTimer` 的实现或调用方式（传入 `seconds / 60.0` 或修改 `runTimer` 接受秒）。

这个方案完全符合您的需求。