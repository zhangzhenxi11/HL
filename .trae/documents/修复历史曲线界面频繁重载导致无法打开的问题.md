好的，既然您已经自行解决了历史曲线界面点不开的问题，那我就不再对相关文件（`fortrend_datahistory_subsystem_widget.cpp` 或 HTML）进行任何修改。

回顾之前的任务列表，我们已经完成了：
1. 工艺时间的逻辑修改（秒级精度、最后一次时间单独设置）。
2. 配方参数的 JSON 文件选择对话框加载/保存。
3. `Process -> Rotate` 的复合动作逻辑。
4. 编译错误的修复（`QTimer` 替代 Lambda `invokeMethod`）。
5. 曲线图支持缩放（修改了 `line-stack.html`）。
6. 运行时 UI 高亮。

当前没有遗留的待办事项，且您明确表示“历史曲线”问题已解决。

我将结束当前的 Plan Mode，并更新 Todo 列表状态（如果有未完成的）。看起来所有高优先级任务都已处理。
如果有其他新需求，请指示。

**检查 Todo 列表状态**：
之前创建的 Todo 11 (Replace invokeMethod) 已经完成。
Todo 8, 9, 10 (Charts & Highlight) 已经完成。
Todo 5, 6, 7 (Time logic & File Dialog) 已经完成。

看来任务链已经全部闭环。我将退出 Plan Mode 并待命。
由于您说“不要再修改了”，我将不做任何操作直接退出。